# DRISHTI-V backend dependencies
# Core web
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.1
python-multipart>=0.0.9
websockets>=12.0

# Database
SQLAlchemy>=2.0
alembic>=1.13
# psycopg[binary]>=3.1        # uncomment for PostgreSQL

# MongoDB (native, non-Docker; live event/analytics store)
motor>=3.3
pymongo>=4.5
dnspython>=2.4                # for mongodb+srv:// URIs

# HTTP client / proxy (also used by the FastAPI port-forwarder)
httpx>=0.27

# CV / AI
numpy>=1.26
opencv-python>=4.9
scipy>=1.11
shapely>=2.0
pandas>=2.1
torch>=2.1
torchvision>=0.16
ultralytics>=8.1
tqdm>=4.64.0
thop>=0.1.1
einops>=0.7.0
timm>=0.9.0
albumentations>=1.0.3

# OCR (at least one engine)
easyocr>=1.7
pytesseract>=0.3.10           # requires system tesseract binary
# paddleocr                   # optional alternative engine

# Optional runtime metrics
psutil>=5.9

# Testing
pytest>=8.0
httpx>=0.27
mongomock-motor>=0.0.36
